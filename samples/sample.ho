package samples

import (
	"log"
)

type Colors enum {
	Red,
	Green,
	Blue
}

type Result enum<T, E> {
	Ok(T),
	Error(E)
}

func init() {
	x := 12345
	x := 123.45
	x := 123.45i
	x := 'a'
	x := "abc"

	colors := {
		"gold": {
			255,
			215,
			0
		}
	}

	log.Println(a, m)
}